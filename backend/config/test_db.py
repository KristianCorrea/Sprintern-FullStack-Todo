from database import create_db_and_tables

# Call this function to create tables
create_db_and_tables()

# Run this script with `python test_db.py` to test tables being created.

# It should create the tables (users and tasks) based on your User and Task models.